using UnityEngine;

namespace droid.Runtime.Prototyping.Sensors {
  public class UberCameraSensor : MonoBehaviour {
    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() { }
  }
}
